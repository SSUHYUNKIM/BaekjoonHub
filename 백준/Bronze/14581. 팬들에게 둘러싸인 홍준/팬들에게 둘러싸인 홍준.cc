#include <iostream>

using namespace std;

string name;

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    
    cin >> name;
    
    cout << ":fan::fan::fan:" << '\n';
    cout << ":fan::" << name << "::fan:" << '\n';
    cout << ":fan::fan::fan:" << '\n';
    return 0;
}